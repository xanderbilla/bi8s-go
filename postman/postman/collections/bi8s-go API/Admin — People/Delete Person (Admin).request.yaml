$kind: http-request
description: |-
  Permanently delete a person record by ID.

  **Path param `peopleId`:** alphanumeric + `-_`, max 100 chars.

  Returns `200` with `{data: null}` on success, `404` if not found.
url: "{{baseUrl}}/a/people/{{peopleId}}"
method: DELETE
order: 4000
